<div class="discussion">
  <%= form_for(@user) do |f| %>
    <% if @user.errors.any? %>
      <div id="error_explanation">
        <h2><%= pluralize(@user.errors.count, "error") %> prohibited this user from being saved:</h2>

        <ul>
        <% @user.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
        </ul>
      </div>
    <% end %>

      <div class="field">
        <p class="inline">Classe :</p>
        <%= f.collection_select( :class_room_id, ClassRoom.all, :id, :label) %>
      </div>

      <%= f.text_field :name, :class => 'input', :placeholder => 'Nom' %>

      <%= f.text_field :firstname, :class => 'input', :placeholder => 'Prénom' %>

      <%= f.text_field :mail, :class => 'input', :placeholder => 'e-mail' %>

      <%= f.text_field :password, :class => 'input', :placeholder => '••••••' %>

      <%= f.text_field :password_confirmation, :class => 'input', :placeholder => '••••••' %>

      <div class="actions">
        <%= f.submit 'S\'inscrire', :id => 'envoyer2' %>
      </div>
  <% end %>
</div>